.DS_Store
.idea
.idea*
static/tmp/*
!static/tmp/.gitkeep
sites
sites/*
sites*
_sites
_sites/*
vendor
vendor/*
*.swp
*.swo
composer.lock
composer.phar

# Documentation site (docs/site/ is the source; the rest of docs/ is built
# from it and IS committed — see docs/.build-manifest)
node_modules
node_modules/*

# Local secrets — never commit. Provide credentials via environment variables.
secrets.php
.env
.env.*
!.env.example
*.pem
*.key
!**/*.pub.key
.htpasswd
